# DECISIONS.yml
# DOD（決定指向開発）用の現在有効な決定事項一覧テンプレート（可変）
# このファイルを、現在有効なプロジェクト決定と実装拘束条件の正本として扱います。
# 人間が一覧を素早く走査できるよう、既定の決定形は軽く保ちます。
# 背景、トレードオフ、却下案、調査メモは records/{discussion-id}.md に記録します。
# records/{discussion-id}.md は議論履歴専用です。
# 更新が前提になる項目（status、残作業、後で更新するアクション項目）を置いてはいけません。
# 決定契約は、現在の実装を拘束するときだけこのファイルに明示します。
# 契約詳細が独立した有効ルールになる場合は、すべてのノードに同じフィールドを強制するのではなく、
# 小さなサブ決定として追加します。

version: "1.0"
decisions:

  # このカテゴリ名は、関連する決定事項をまとめる関心領域またはドメイン名に置き換えます。
  # 既定の例としては Feature、Infrastructure、Business Logic、Security、Data、CI/CD が使えます。
  # トップレベルは「どの領域の決定か」を表すために使います。
  # 仕様・設計・実装・テストの関心は、それ自体が実装を拘束する場合に限り、
  # その領域の中の決定事項またはサブ決定として表現します。
  カテゴリ名:
    - # 必須。{category}-{sequence}-{shortname} のような安定したIDを使います。
      id: category-001-short-name
      # 必須。決定内容を1行で書きます。
      title: 決定内容を1行で書く
      # 必須。現在の実装を拘束する理由を、簡潔に1〜3行程度で書きます。
      # 議論の全履歴ではなく、今効いている拘束理由を書くことが重要です。
      reason: この決定が今の実装を拘束する理由を書く
      # 任意。基本的には次の既定ステータスを使います。
      # - ⚠️Discussion In Progress
      # - ⚠️Discussion Approved
      # - ⚠️Implementing
      # - ✅️Implementation Approved
      # ⛔️On Hold や ⛔️Cancelled は本当に必要な場合だけ使います。
      status: "⚠️Discussion In Progress"
      # 任意。1つの議論から生まれた決定群であれば、その議論履歴ファイルを指します。
      link: "records/category-001-discussion-topic.md"
      # 任意。契約詳細やルールが独立して有効になったときだけ子決定を追加します。
      # 不変条件、非目標、受け入れ基準、失敗条件は、それ自体を個別参照する必要がある場合に限り
      # サブ決定として表現します。
      # 例:
      # sub_decisions:
      #   - id: category-001-1-contract-detail
      #     title: 独立して有効な契約詳細を1つ書く
      #     reason: この詳細が今の実装を拘束する理由を書く
      #     status: "⚠️Discussion Approved"
      #     sub_decisions: []
      sub_decisions: []

  # 例:
  # Feature:
  #   - id: feature-001-profile-editing
  #     title: ユーザープロフィール編集機能
  #     reason: この機能が現在の UI、API、バリデーション実装を横断して拘束している
  #     status: "⚠️Implementing"
  #     link: "records/feature-001-profile-editing.md"
  #     sub_decisions:
  #       - id: feature-001-1-spec-fields
  #         title: 編集可能項目とバリデーション規則
  #         reason: 実装前に API とフォームスキーマで許可項目を一致させる必要がある
  #         status: "⚠️Discussion Approved"
  #         sub_decisions: []
  #       - id: feature-001-2-design-boundary
  #         title: サーバーとクライアントの責務境界
  #         reason: コンポーネント構成と更新フローがこの境界に依存する
  #         status: "⚠️Implementing"
  #         sub_decisions: []
  #       - id: feature-001-3-test-acceptance
  #         title: 正常系と拒否系の受け入れテスト範囲
  #         reason: 正しい更新成功と不正入力拒否を検証しない限り、この機能は完了と見なせない
  #         status: "⚠️Discussion Approved"
  #         sub_decisions: []

  # 必要に応じてカテゴリや決定事項を増やします。
  # 大きな1件を肥大化させるより、小さな決定を複数並べるほうを優先します。
  # 次の実装判断を変えうるルールは、すべてこのファイルに存在させます。